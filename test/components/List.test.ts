import Contact from './Contact';

describe('List Component Test', () => {
  it('isolated List.tsx component should render without crashing', () => {
    console.log('no test for about page...');
  });
});
