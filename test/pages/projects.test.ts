import { MemoryRouter } from 'react-router-dom';

describe('Projects Page', () => {
  it(' /pages/projects.tsx page should render all of its child components without crashing', () => {
    console.log('about test undefined');
  });
});
